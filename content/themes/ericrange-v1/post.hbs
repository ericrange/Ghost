{{!< default}}

{{! The comment above "< default" means - insert everything in this file into
    the {body} of the default.hbs template, which contains our header/footer. }}

<nav class="main-nav clearfix">
    <paper-button class="back">
        <a href="/">Home</a>
    </paper-button>
    {{> "menu"}}
</nav>

<main class="content" role="main" itemscope itemprop="mainContentOfPage" itemtype="http://schema.org/Blog">

    <article class="{{post_class}}" itemscope itemType="http://schema.org/BlogPosting">
    {{#post}}

        <header class="post-header">
            <h1 class="post-title" itemprop="headline">{{title}}</h1>
            <div class="post-meta">
                <time class="post-date" datetime="{{date format='YYYY-MM-DD'}}" itemprop="datePublished">{{date format="DD. MMMM YYYY"}}</time> <span class="tags" itemprop="keywords">{{tags prefix=" in "}}</span> von <span class="author">{{author.name}}</span>
            </div>
        </header>

        <section class="post-content" itemprop="articleBody">
            {{content}}
        </section>

        <footer role="contentinfo" class="post-footer">

        {{! Everything inside the #author tags pulls data from the author }}
        <section class="author" itemprop="author">
            <div itemscope itemtype="http://schema.org/Person">
                {{#author}}

                    {{#if image}}
                    <figure class="author-image" itemprop="image">
                        <a class="img" href="{{url}}" style="background-image: url({{image}})"><span class="hidden">{{name}}'s Picture</span></a>
                    </figure>
                    {{/if}}


                        <h4><a href="{{url}}" itemprop="name">{{name}}</a></h4>

                        {{#if bio}}
                            <p>{{bio}}</p>
                        {{else}}
                            <p>Read <a href="{{url}}">more posts</a> by this author.</p>
                        {{/if}}
                        <div class="author-meta">
                            {{#if location}}<span class="author-location icon-location" itemprop="homeLocation">{{location}}</span>{{/if}}
                            {{#if website}}<span class="author-link icon-link"><a href="{{website}}">{{website}}</a></span>{{/if}}
                        </div>

                {{/author}}
            </div>
        </section>

        <social-share headline="Teile mich ..." class="share" shareSize="32" shareUrl="{{url absolute="true"}}" shareTitle="{{encode title}}"></social-share>

        </footer>

    {{/post}}
    </article>

    <section class="subscription">
        <div class="container">
            <h4 class="title"><span class="mega-octicon octicon-rss"></span>Abonnieren</h4>
            <p>If I write shit, you get it in your inbox a maximum of 1 time per week. If I don't then nothing happens.</p>
            <paper-input type="email" id="subscribe-email-footer" label="E-Mail Adresse" error="Das ist keine E-Mail Adresse!"></paper-input>
            <paper-button onclick="Subscribe.apply('subscribe-email-footer');" autofocus>Abonnieren</paper-button>
        </div>
    </section>

</main>
